version: "3.9"

services:
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "4000:3000"
    env_file:
      - ./backend/.env